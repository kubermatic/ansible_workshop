# ansible_workshop

* Clone the repo
* ```bash
  cd ansible_workshop/vagrant
  vagrant up
  vagrant ssh node-01
```
* On the VM
* ```bash
  cd /vagrant/ansible
  ansible-playbook plays/wordpress.yaml
```
